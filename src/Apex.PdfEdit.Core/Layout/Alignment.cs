namespace Apex.PdfEdit.Core.Layout;

public enum Alignment
{
    Left,
    Center,
    Right,
    Justified,

    /// <summary>Node has no content or falls outside all classification thresholds.</summary>
    Unknown
}
